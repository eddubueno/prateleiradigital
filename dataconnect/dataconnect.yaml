specVersion: "v1"
serviceId: "prateleira-nova"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "prateleira-nova-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
